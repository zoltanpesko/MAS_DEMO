apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: mas-vendor-page
  labels:
    app: mas-vendor-page
    app.kubernetes.io/name: mas-vendor-page
    app.kubernetes.io/component: frontend
spec:
  output:
    to:
      kind: ImageStreamTag
      name: mas-vendor-page:latest
  source:
    type: Git
    git:
      uri: https://github.com/zoltanpesko/MAS_DEMO.git
      ref: main
    contextDir: frontend
  strategy:
    type: Docker
    dockerStrategy:
      dockerfilePath: Dockerfile
      noCache: false
      env:
      - name: NODE_ENV
        value: production
  triggers: []
  # Triggers disabled - builds are started manually by the deployment script
  # This prevents automatic builds when BuildConfig is created
  successfulBuildsHistoryLimit: 5
  failedBuildsHistoryLimit: 5

# Made with Bob
