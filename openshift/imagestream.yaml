apiVersion: image.openshift.io/v1
kind: ImageStream
metadata:
  name: mas-vendor-page
  labels:
    app: mas-vendor-page
    app.kubernetes.io/name: mas-vendor-page
    app.kubernetes.io/component: frontend
spec:
  lookupPolicy:
    local: false
  tags:
  - name: latest
    annotations:
      description: The latest build of MAS Vendor Page
    from:
      kind: DockerImage
      name: mas-vendor-page:latest

# Made with Bob
